pydantic>=2.6

# Optional: only needed for real Gemini calls (GEMINI_API_KEY set).
# Without it (or without the key), MockConceptGenerator is used automatically.
# NOTE: google-generativeai is fully deprecated -- use the unified google-genai SDK.
google-genai>=1.0

# Dev/test only
pytest>=8.0

# Person 1 trained XGBRegressor runtime (no notebook dependencies).
xgboost==3.2.0
pandas
joblib
scikit-learn
