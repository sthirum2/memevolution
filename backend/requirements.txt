fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
sqlalchemy>=2.0,<3
pydantic-settings>=2.0,<3
psycopg[binary]>=3.2,<4
# rendering memes to a real JPEG for publishing
Pillow>=10,<13
# AI image + video generation (same key as the agent's concept generation)
google-genai>=1.0
# optional standalone still-to-video rendering utility;
# bundles a static ffmpeg binary so nobody needs a system install
imageio-ffmpeg>=0.5
